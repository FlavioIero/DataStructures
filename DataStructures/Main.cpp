#include <iostream>
#include <cstdlib>
#include <vector> // not necessary, only to copy names and behaviors
#include <ctime> // just for tests
#include "SinglyLinkedList.h"
#include "OrderedMap.h"
using namespace std;


int main()
{
    srand(time(0));

    OrderedMap<string, int> bst = OrderedMap<string, int>();

    return 0;
}
